mutation registerAdmin ($firstName: String!, $lastName: String!, $email: String!, $password: String!) {
  signUp (credentials: { firstName: $firstName, lastName: $lastName, email: $email, password: $password }) {
    id
    email
    firstName
    lastName
  }
}
